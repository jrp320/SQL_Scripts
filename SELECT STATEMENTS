SELECT * FROM film;

/* slecting first name, last name, and email */
SELECT first_name, last_name, email 
FROM customer;

/* Show all different film relase years  */
SELECT DISTINCT release_year 
FROM film; 

/* Show all different rental rates for the films  */
SELECT DISTINCT rental_rate 
FROM film; 

/* (Challenge) get all the rating types our films can have */
SELECT DISTINCT rating
FROM film;


/* Using WHERE statements */

SELECT email
FROM customer
WHERE first_name = 'Jared';

/* Show all payments of 7.99 */
SELECT *
FROM payment
WHERE amount = 7.99;

/* Show all payments less than $5 */
SELECT *
FROM payment
WHERE amount < 5;

/* Show all payments not equal to $5 */
SELECT *
FROM payment
WHERE amount != 5;





